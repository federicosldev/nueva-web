---
import MarqueeText from './MarqueeText.astro';

const scrollItems = ['DESARROLLO WEB', 'DISEÑO', 'MARKETING DIGITAL'];
---

<section class="px-4 sm:px-6 lg:px-12 max-w-[100vw] lg:h-[600px]">
    <div class="max-w-full mx-auto">
        <h1 class="sr-only">Creando experiencias digitales</h1>

        <div class="space-y-2 sm:space-y-4 md:space-y-6">
            <div
                class="flex items-start md:items-start gap-4 sm:gap-8 md:gap-12 md:mt-10"
            >
                <span
                    class="font-title text-6xl sm:text-6xl md:text-8xl lg:text-9xl xl:text-[14rem] leading-6 lg:leading-[0.8] text-primary-pink uppercase"
                >
                    Creando
                </span>
                <img
                    class="h-10 sm:h-16 md:h-20 lg:h-24 xl:h-32 mt-[-14px] md:mt-0 mb-[16px] md:mb-0"
                    src="/hero-smile.svg"
                    alt="Icono investigación, desarrollo, diseño"
                    id="hover-effect-1"
                />
            </div>
            <div
                id="floating-scroll-1"
                class="hidden fixed pointer-events-none z-50"
            >
                <div
                    class="relative w-48 h-48 bg-white rounded-full flex items-center justify-center shadow-lg overflow-hidden border border-dark p-2"
                >
                    <div
                        class="absolute w-full h-full animate-spin-slow flex items-center justify-center p-5"
                    >
                        <img
                            src="/text-scroll-smooth.svg"
                            alt="Scroll"
                            class="rotating-image absolute w-full h-full object-contain p-1"
                        />
                    </div>
                    <img
                        class="h-24 w-24 ml-2"
                        src="/scroll-smooth.svg"
                        alt="Efecto hover hero section"
                    />
                </div>
            </div>
            <div
                id="floating-scroll-2"
                class="hidden fixed pointer-events-none z-50"
            >
                <div
                    class="relative w-48 h-48 bg-white rounded-full flex items-center justify-center shadow-lg overflow-hidden border border-dark p-2"
                >
                    <div
                        class="absolute w-full h-full animate-spin-slow flex items-center justify-center p-5"
                    >
                        <img
                            src="/efecto-hover-hero.svg"
                            alt="Scroll"
                            class="rotating-image absolute w-full h-full object-contain p-1"
                        />
                    </div>
                    <img
                        class="h-24 w-24 ml-5"
                        src="/mano-keep-scrolling.svg"
                        alt="Efecto hover hero section 2"
                    />
                </div>
            </div>
            <div class="flex items-center gap-4 sm:gap-8 md:gap-12">
                <img
                    class="h-10 sm:h-16 md:h-20 lg:h-24 xl:h-40"
                    src="/good-vibes.svg"
                    alt="Icono investigación, desarrollo, diseño"
                    id="hover-effect-2"
                />
                <span
                    class="font-title text-6xl sm:text-6xl md:text-8xl lg:text-9xl xl:text-[14rem] leading-6 lg:leading-[0.8] text-primary-purple uppercase"
                >
                    experiencias
                </span>
            </div>

            <div
                class="flex flex-col md:flex-row items-start md:items-center gap-4 sm:gap-8 md:gap-12"
            >
                <span
                    class="font-title text-6xl sm:text-6xl md:text-8xl lg:text-9xl xl:text-[14rem] leading-12 lg:leading-[0.8] text-primary-pink uppercase"
                >
                    digitales
                </span>
                <p
                    class="font-paragraph text-xl sm:text-lg md:text-xl lg:text-2xl xl:text-3xl max-w-[650px] mt-4md:mt-0"
                >
                    Experiencias digitales que enganchan, posicionan y venden.
                    Porque tu proyecto merece más que una web bonita. Creo
                    productos digitales que enamoran, funcionan y hacen que tu
                    proyecto despegue.
                </p>
            </div>
        </div>
    </div>
</section>

<div class="relative w-full mt-8 sm:mt-12 md:mt-20 md:h-[300px]">
    <section class="transform -rotate-6 w-full">
        <MarqueeText items={scrollItems} />
    </section>

    <section class="transform rotate-6 w-full -mt-12 sm:-mt-16 md:-mt-28">
        <MarqueeText items={scrollItems} direction="reverse" />
    </section>
</div>

<script>
    function initScrollEffect() {
        const floatingScroll1 = document.getElementById('floating-scroll-1');
        const floatingScroll2 = document.getElementById('floating-scroll-2');
        const hoverImages1 = document.querySelectorAll('img#hover-effect-1');
        const hoverImages2 = document.querySelectorAll('img#hover-effect-2');

        let isHovering1 = false;
        let isHovering2 = false;
        let scrollTimeout;

        const isDesktop = window.matchMedia('(min-width: 1024px)').matches;

        function updateFloatingScrollPosition(e, floatingScroll) {
            if (isDesktop) {
                floatingScroll.style.left = `${e.clientX}px`;
                floatingScroll.style.top = `${e.clientY}px`;
            }
        }

        function showFloatingScroll(floatingScroll) {
            if (isDesktop) {
                floatingScroll.classList.remove('hidden');
            }
        }

        function hideFloatingScroll(floatingScroll) {
            if (isDesktop) {
                floatingScroll.classList.add('hidden');
            }
        }

        function hideAllFloatingScrolls() {
            hideFloatingScroll(floatingScroll1);
            hideFloatingScroll(floatingScroll2);
        }

        if (isDesktop) {
            hoverImages1.forEach(img => {
                img.addEventListener('mouseenter', () => {
                    isHovering1 = true;
                    isHovering2 = false;
                    hideAllFloatingScrolls();
                    showFloatingScroll(floatingScroll1);
                });
                img.addEventListener('mouseleave', () => {
                    isHovering1 = false;
                    hideFloatingScroll(floatingScroll1);
                });
            });

            hoverImages2.forEach(img => {
                img.addEventListener('mouseenter', () => {
                    isHovering2 = true;
                    isHovering1 = false;
                    hideAllFloatingScrolls();
                    showFloatingScroll(floatingScroll2);
                });
                img.addEventListener('mouseleave', () => {
                    isHovering2 = false;
                    hideFloatingScroll(floatingScroll2);
                });
            });

            document.addEventListener('mousemove', e => {
                if (isHovering1) {
                    updateFloatingScrollPosition(e, floatingScroll1);
                } else if (isHovering2) {
                    updateFloatingScrollPosition(e, floatingScroll2);
                }
            });

            document.addEventListener('scroll', () => {
                if (isHovering1 || isHovering2) {
                    clearTimeout(scrollTimeout);
                    scrollTimeout = setTimeout(() => {
                        hideAllFloatingScrolls();
                    }, 1000);
                }
            });
        }
    }

    document.addEventListener('DOMContentLoaded', initScrollEffect);
    document.addEventListener('astro:page-load', initScrollEffect);
</script>
